<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.CookingMama.dev.repository.ItemRegistRepository">
    <insert id="insertItemRegist">
        INSERT INTO
            items(admin_id, category, item_name, item_option, item_price, item_image, item_info, item_count)
        VALUES
            (#{adminId}, #{category}, #{itemName}, #{itemOption}, #{itemPrice}, #{itemImage}, #{itemInfo}, #{itemCount})
    </insert>
</mapper>